spring:
  profiles:
    active: prod
  config:
    import: application-aws.yml
  application:
    name: git-turl

  ai:
    openai:
      api-key: ${OPENAI_KEY}

      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.2
